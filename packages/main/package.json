{
  "name": "@os-browser/main",
  "version": "1.0.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --outdir=dist --external:electron",
    "dev": "esbuild src/main.ts --bundle --platform=node --outdir=dist --external:electron --watch"
  },
  "dependencies": {
    "@os-browser/shared": "*",
    "sql.js": "^1.11.0",
    "electron-updater": "^6.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
